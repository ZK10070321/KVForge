# Day 4 复习：完整问题、参考答案与易错点

阅读方式：先掌握1～8题的实验定义，再复习9～18题的计时，最后阅读结果解释。
本页记录当前代码的实际口径，不把CPU微基准写成GPU或线上服务成绩。

## 1. 为什么使用固定 token 回放，而不是分别随机生成？

答：提前固定相同token ID，使cache/no-cache处理相同前缀与工作量，避免采样开销和输入差异影响比较。只前向，不计算训练loss、不反向、不更新权重。

易错：固定输入不等于训练；这个实验不评价文本质量。

## 2. P、N、B是什么？两条路径分别输入什么？

答：P是提示词长度，N是额外decode调用次数，B是batch。准备[B,P+N]的ID。以P=3、N=2为例，prefill两者输入[a,b,c]；随后无缓存输入[a,b,c,d]、[a,b,c,d,e]，缓存依次输入[d]、[e]并读取历史KV。

易错：缓存版新增输入短，但可见上下文相同。N不是generate函数端到端输出token数的同一口径。

## 3. 为什么要求P+N不超过窗口？

答：本实验测窗口内增量计算；超窗重建会混入完整窗口重算，因此明确报错，不自动改变工作量。

易错：generate允许重建，不表示benchmark也应该悄悄重建。

## 4. 怎样计算吞吐、每步时间和加速比？

答：设decode总时间为t秒，吞吐=B×N/t；每批一步平均时间=1000t/N毫秒；加速比=无缓存时间/缓存时间。B=4、N=20、t=0.5秒时，处理80个新增位置，吞吐160 token/s，每步25毫秒。

易错：每步时间不再除B；0.025秒是25毫秒。若120毫秒降为80毫秒，加速1.5倍，耗时下降33.33%，吞吐提高50%。

## 5. 为什么每步延迟不除以batch？

答：一步同时完成整批样本，但整批仍经历这一步的等待时间。t/(B×N)是摊销计算成本，不是本项目定义的批次每步时间。

易错：吞吐高不等于每个请求延迟低。

## 6. 为什么prefill不一定因缓存变快？

答：首次没有已算好的提示词历史可复用，两条路径都要计算提示词，缓存版还要写KV。主要复用收益发生在后续decode。

易错：本项目prefill计时不含缓存申请，也不含排队、tokenizer或网络，不能当作完整服务TTFT。

## 7. 随机权重模型能否用于性能测试？

答：可以测指定形状与实现的计算成本，但须标注随机权重与合成输入。不能据此评价语言能力或证明MHA/GQA质量相同。不同头数也可能改变参数量。

易错：性能实验不等于模型训练或质量消融。

## 8. 如何保证cache/no-cache对照公平？

答：同一对照使用同一个模型、权重、设备、精度、后端、batch与固定输入；先核对logits；排除窗口重建；输入搬运、采样和打印不进计时；两条路径都预热；每轮新缓存；交替顺序；多次记录中位数和全部原始样本。

易错：随机种子不保证耗时完全相同，系统负载仍会变化。

## 9. 生成看起来正常，为什么还比较logits？

答：错误实现可能减少了必要计算，既快又错。不同logits也可能具有相同argmax，生成相同文本。例如[5,3,1]与[4,3.9,2]都选第一个token。因此先比较所有相关位置的logits，确认数值在合理容差内，再输出性能。

易错：检查不通过应排错，不能随意扩大容差。

## 10. CUDA计时为什么在开始前和结束前同步？

答：CUDA操作可能异步，CPU提交任务后返回不等于GPU完成。开始前同步避免上一阶段未完成任务混入；结束读表前同步确保本次工作完成，否则可能只测到提交开销。

易错：synchronize不是清空显存，也不是把模型移到CPU。perf_counter加同步测墙钟时间，包含Python调度，不是纯kernel时间。

## 11. 当前prefill时间包含缓存申请吗？

答：不包含。run_trial先申请缓存，随后开始prefill计时；但是显存统计在申请前开始，因此缓存申请纳入本轮峰值统计。

易错：时间与内存的统计边界不同，必须明确说明。

## 12. 预热有什么作用？warmup=0就是冷启动吗？

答：先运行一些不计成绩的轮次，使常见初始化等发生后再测量。当前脚本在预热前已经执行正确性检查，因此warmup=0也不是纯冷启动实验。

易错：预热不能完全消除噪声；benchmark预热与训练学习率warmup完全不同。

## 13. 为什么每轮重新创建缓存？

答：确保每轮从相同空历史开始，经历相同prefill和N步decode。直接沿用上一轮会改变历史长度、位置、上下文，可能导致越界或错误工作量。

易错：重用模型权重合理，重用已经填满的请求历史不合理。

## 14. 为什么交替cache/no-cache执行顺序？

答：降低某条路径总是先执行造成的顺序偏差，例如设备状态或系统负载随时间变化。不能保证消除所有干扰，所以还需重复和保留原始样本。

易错：交替顺序不等于随机化所有实验条件。

## 15. 为什么用中位数并保留原始样本？

答：中位数对少量极端值不敏感，适合描述这组样本的典型时间；原始样本用于识别抖动和检查统计。8、9、10、9、64毫秒排序为8、9、9、10、64，中位数9毫秒，平均数20毫秒。

易错：中位数不能说明尾延迟；不能只选最快一次或删除慢样本。五轮也不足以可靠描述线上P99。

## 16. baseline、peak和extra分别是什么？

答：baseline是本轮缓存申请前的PyTorch已分配张量内存；peak是本轮申请、prefill、decode期间的峰值；extra=peak−baseline。baseline=200MiB、peak=270MiB时extra=70MiB，它可能包含缓存与临时张量，不一定等于40MiB的KV缓存。

易错：extra不是专门隔离出的KV大小。allocated不同于reserved，也不同于nvidia-smi进程总占用。

## 17. reset_peak_memory_stats会释放内存吗？CPU null是什么意思？

答：不会释放，它重置峰值统计。CPU报告GPU字段为null，表示未测，不是占用0字节。

易错：不要与KVCache.reset混淆；后者重置有效长度，同样不释放预分配张量。

## 18. 平均每步2毫秒等于线上P99为2毫秒吗？

答：不是。本指标来自一段固定token回放总时间除以步数，不是逐请求/逐token延迟分布；没有线上排队、网络、并发、调度或采样。P99需明确测量对象，收集足够真实样本并计算99百分位。

易错：一段平均值、重复轮次中位数和P99是不同统计量。

## 19. JSON与CSV分别保存什么？

答：JSON保存嵌套环境、配置、模型来源、正确性信息和每轮样本；CSV以平面表格保存每组每种模式的汇总指标，方便筛选。18组各有两种模式，所以CSV有36条数据行，不含表头。

易错：CSV不是所有原始样本的替代品；键名、单位和模型来源要一起看。

## 20. 四张子图怎样解读？

答：吞吐越高越快；加速比大于1表示缓存更快；prefill衡量提示词计算；缓存KiB是预分配KV字节数而非总内存。同色对应同KV头数，吞吐/prefill虚线无缓存、实线有缓存。

易错：两张PNG分别是naive/SDPA，每张四个子图；不要只挑最大加速点。

## 21. 上下文变长，为什么缓存优势可能更明显？

答：基线每步重算增长的完整前缀，重复工作增加；缓存只计算新增位置并读取历史，因此可能获得更大相对收益。实际大小取决于硬件、模型、实现和长度。

易错：缓存仍需读历史，不能推断其成本恒定或优势一定单调增长。

## 22. GQA缓存更小，是否一定更快？SDPA是否一定是FlashAttention？

答：都不能保证。减少KV头数能降低常驻缓存，但运行还受临时扩展、计算、调度和硬件影响。SDPA是调用接口，实际实现取决于设备、dtype、mask等条件；本次CPU结果没有证明使用FlashAttention。

易错：缓存减少比例不等于总显存或时间减少比例。

## 23. 测试全通过，是否说明benchmark结果能推广到所有机器？

答：不能。测试检查工作量、公式、文件与错误处理等逻辑；成绩只属于记录的环境与配置。GPU或新版本需要另行验证。

易错：不在单元测试里要求缓存必须更快；这会让测试受硬件噪声影响。

## 24. 如何介绍Day 4成果？

答：实现固定token推理微基准，先验证logits，再分别测prefill/decode，控制权重、输入、窗口、预热与顺序，保存原始样本及JSON/CSV/图表。已完成CPU下MHA/GQA/MQA和两种后端的18组对照，以及真实checkpoint短窗口对照。GPU接口提供但尚未实测，不宣称线上服务性能或质量提升。

## 本次用户复现实测（2026-09-13）

来源：`runs/day4_verify/results.json`、`runs/day4_checkpoint_verify/results.json`及用户终端输出。
环境：PyTorch2.5.1+cpu、Python3.11.7、FP32、CPU线程2。
专项5项、总计23项测试通过；18组实验与4组checkpoint实验成功，两个PNG生成成功。

| SDPA / GQA Hkv=2 | 无缓存tok/s | 缓存tok/s | 加速比 |
|---|---:|---:|---:|
| P=32 | 632.4 | 840.9 | 1.330× |
| P=128 | 320.5 | 691.9 | 2.159× |
| P=256 | 246.2 | 824.2 | 3.347× |

全18组约1.285～3.707×，旧checkpoint短窗口约1.055～1.125×。不同运行波动正常，不能据此承诺任何GPU成绩。P=128到256缓存吞吐上升也不能解释成“历史越长缓存绝对速度越快”；需要更多重复测量才能分析。

## 建议复习顺序

先用自己的话回答：测什么→怎么保证公平→怎样计算指标→结果能说明什么。
再背计时细节。不会时先看一个具体数字例子，不必一次记住全部API。
